
import React from 'react';
import { View, Text, StyleSheet, TouchableOpacity } from 'react-native';

export default function App() {
  return (
    <View style={styles.container}>
      <Text style={styles.logo}>TASKA</Text>

      <Text style={styles.title}>
        Super App na Hausa
      </Text>

      <Text style={styles.subtitle}>
        Wallet • Data • AI • Airtime
      </Text>

      <TouchableOpacity style={styles.button}>
        <Text style={styles.buttonText}>
          Fara Yanzu
        </Text>
      </TouchableOpacity>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#16a34a',
    justifyContent: 'center',
    alignItems: 'center',
    padding: 20,
  },

  logo: {
    fontSize: 45,
    color: 'white',
    fontWeight: 'bold',
    marginBottom: 20,
  },

  title: {
    fontSize: 28,
    color: 'white',
    fontWeight: 'bold',
    textAlign: 'center',
  },

  subtitle: {
    fontSize: 16,
    color: '#dcfce7',
    marginTop: 10,
    textAlign: 'center',
    marginBottom: 40,
  },

  button: {
    backgroundColor: 'white',
    paddingVertical: 15,
    paddingHorizontal: 40,
    borderRadius: 12,
  },

  buttonText: {
    color: '#16a34a',
    fontSize: 18,
    fontWeight: 'bold',
  },
});
